server:
  port: 0
spring:
  datasource:
    username: test
    password: test
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5434/shopping-cart-db
  sql:
    init:
      mode: always

